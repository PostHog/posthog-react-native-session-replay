# Force a hoisted node_modules layout in the example so React Native's iOS
# autolinking and CocoaPods see a flat dependency tree instead of pnpm's
# symlink layout. The symlinked layout causes duplicate-output errors when
# Xcode copies headers from multiple Pods that share node_modules paths.
node-linker=hoisted
